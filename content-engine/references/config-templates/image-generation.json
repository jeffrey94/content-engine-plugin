{
  "backend": "prompt-only",
  "backends": {
    "prompt-only": {
      "description": "Save prompt markdown files only. No image generation API called.",
      "enabled": true,
      "output": "prompts-only"
    },
    "gemini": {
      "description": "Use Google Gemini image generation via API",
      "enabled": false,
      "api_key_env": "GEMINI_API_KEY",
      "model": "gemini-2.5-flash-image",
      "models": {
        "flash": {
          "id": "gemini-2.5-flash-image",
          "description": "Fast drafts, batch generation. Lower latency, good quality.",
          "tier": "default"
        },
        "pro": {
          "id": "gemini-3-pro-image-preview",
          "description": "Final assets, 4K support, advanced reasoning. Preview.",
          "tier": "quality"
        }
      },
      "max_images_per_run": 10,
      "output_format": "png"
    }
  },
  "fallback_behavior": "If selected backend fails or API key is missing, fall back to prompt-only mode and notify user.",
  "output_directory_pattern": "data/images/YYYYMMDD/{article-slug}/"
}
